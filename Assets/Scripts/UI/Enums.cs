namespace UI
{
    public static class Enums
    {
        public enum ScreenType
        {
            LoadingScreen,
            MainMenu
        }

        public enum CanvasType
        {
            Overlay,
            WorldSpace
        }
    }
}